const config = {
  // For production, this will be an empty string to use relative URLs
  // For development, uncomment the line below
  // API_BASE_URL: 'http://localhost:5001',
  API_BASE_URL: 'http://localhost:5001'
};

export default config;
